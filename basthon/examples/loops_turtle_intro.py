from turtle import *

shape("turtle")       # Give the pointer a turtle shape
color("limegreen")    # Make the turtle lime green
forward(80)           # Move forward 80 pixels
left(60)              # Turn 60 degrees to the left

done()
